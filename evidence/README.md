# Evidence

This directory stores reproducible validation evidence for Iris Agent R0/R1-P0
work. Evidence files are committed only when they contain sanitized command
output and test reports. Secrets, full prompts, reasoning text, user content and
private runtime data are never written here.
